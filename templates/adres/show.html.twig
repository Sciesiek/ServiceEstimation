{% extends 'base.html.twig' %}

{% block title %}Adres{% endblock %}

{% block body %}
    <h1>Adres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adre.id }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ adre.city }}</td>
            </tr>
            <tr>
                <th>Street</th>
                <td>{{ adre.street }}</td>
            </tr>
            <tr>
                <th>HousingNumber</th>
                <td>{{ adre.housingNumber }}</td>
            </tr>
            <tr>
                <th>Postal</th>
                <td>{{ adre.postal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adres_index') }}">back to list</a>

    <a href="{{ path('adres_edit', {'id': adre.id}) }}">edit</a>

    {{ include('adres/_delete_form.html.twig') }}
{% endblock %}
