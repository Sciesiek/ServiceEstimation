<form method="post" action="{{ path('adres_delete', {'id': adre.id}) }}" onsubmit="return confirm('Czy jesteś pewien że chcesz usunąć ten adres?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adre.id) }}">
    <button class="btn">Usuń</button>
</form>
